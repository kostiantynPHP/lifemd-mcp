# MCP Server Configuration
# Copy this file to .env and update with your actual values

# Required: Base URL of your external API
API_BASE_URL=https://api.yourservice.com

# Optional: API Key for 'api-key' header (if your API requires it)
# Leave empty if not needed
API_KEY=your-api-key-here

# Optional: Static Bearer token for 'authorization' header
# Only needed if your API uses a fixed token that doesn't change
# If you use dynamic token generation via api_auth tool, leave this empty
AUTH_TOKEN=

# Optional: Request timeout in milliseconds (default: 30000)
API_TIMEOUT=30000

# Optional: Server port (default: 3000)
PORT=3000

# Example configurations:

# For APIs with static Bearer token:
# API_BASE_URL=https://api.yourservice.com
# API_KEY=12345
# AUTH_TOKEN=bearer-token-here

# For APIs with dynamic token (patient authorization):
# API_BASE_URL=https://api.yourservice.com
# API_KEY=12345
# AUTH_TOKEN=  (leave empty, token will be set via api_auth tool)

# For testing (no authentication required):
# API_BASE_URL=https://jsonplaceholder.typicode.com
# API_KEY=
# AUTH_TOKEN=
